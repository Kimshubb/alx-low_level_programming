#include<stdio.h>
/*print an output on a newline. Note the puts functions returns a string that can be read using the scanf and gets function on the terminal. formatting characters return errors */
int main(void){
	puts("Programming is like building a multilingual puzzle");
	return (0);
}
